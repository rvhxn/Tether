import React from 'react';
import { View, Text } from 'react-native';
import styles from './FixFiltersSaveTether.module.css';

const FixFiltersSaveTether = () => {
    return (
        <View style={styles.container}>
            <Text>FixFiltersSaveTether Component</Text>
        </View>
    );
};

export default FixFiltersSaveTether;
